from models import session, Trick

# def capture_tricks(x, y, z):
#     new_trick = Trick(steeze = None, user_id = None, game_id = None)
#     save_tricks(new_trick)

# def save_tricks(trick):
#     session.add(trick)
#     session.commit()